/*
            Terminal s;
            do {
                aLex.scanner();
                s = aLex.getS();
                System.out.println(s + " " + aLex.getCad());
            } while (s != Terminal.EOF); util para debuggear aLex
            aSin.parser();
            */

            colocar luego de crear los analizadores y el generador de codigo para testear aLex y aSem